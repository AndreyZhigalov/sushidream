import React from 'react';

export const Profile: React.FC = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
        fontSize: '70px',
      }}>
      Profile
    </div>
  );
};
